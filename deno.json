{
  "tasks": {
    "dev": "denoflare serve examples/worker.ts --do-namespace-binding storage:local:WorkerStorage",
    "test": "deno test --allow-write --allow-run --allow-net",
    "unit": "deno task test modules/",
    "integration": "deno task test integration/",
    "ci": "deno fmt && deno lint && deno task unit --fail-fast && deno task integration --fail-fast"
  }
}
